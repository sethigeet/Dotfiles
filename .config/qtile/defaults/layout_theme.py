from .colors import colors

layout_theme = {
    "border_width": 2,
    "margin": 6,
    "border_focus": colors["window"]["border"]["active"],
    "border_normal": colors["window"]["border"]["inactive"]
}
